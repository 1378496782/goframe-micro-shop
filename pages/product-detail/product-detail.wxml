<!-- 加载状态 -->
<view class="loading" wx:if="{{loading}}">
  <text>加载中...</text>
</view>

<!-- 商品图片轮播 -->
<view class="product-gallery" wx:if="{{!loading && product}}">
  <image src="{{currentImage}}" mode="aspectFit" class="main-image" />
  <scroll-view class="thumbnail-list" scroll-x="true">
    <image 
      wx:for="{{product.images}}" 
      wx:key="index" 
      src="{{item}}" 
      class="thumbnail {{currentImageIndex === index ? 'active' : ''}}"
      bindtap="selectImage" 
      data-index="{{index}}"
    />
  </scroll-view>
</view>

<!-- 商品信息 -->
<view class="product-info" wx:if="{{!loading && product}}">
  <view class="price-section">
    <text class="current-price">¥{{product.price}}</text>
    <text class="original-price">¥{{product.originalPrice}}</text>
    <text class="discount">{{product.discount}}折</text>
  </view>
  
  <text class="product-title">{{product.name}}</text>
  
  <view class="sales-info">
    <text>销量: {{product.sales}}件</text>
    <text>评价: {{product.reviews}}条</text>
    <text>库存: {{product.stock}}件</text>
  </view>
</view>

<!-- 规格选择 -->
<view class="spec-section" wx:if="{{!loading && product}}">
  <text class="section-title">选择规格</text>
  <view class="spec-options">
    <view class="spec-item" wx:for="{{product.specs}}" wx:key="name">
      <text class="spec-name">{{item.name}}</text>
      <view class="spec-values">
        <text 
          wx:for="{{item.values}}" 
          wx:key="*this" 
          class="spec-value {{selectedSpecs[item.name] === item ? 'active' : ''}}"
          bindtap="selectSpec" 
          data-spec-name="{{item.name}}" 
          data-value="{{item}}"
        >
          {{item}}
        </text>
      </view>
    </view>
  </view>
</view>

<!-- 商品详情 -->
<view class="product-description" wx:if="{{!loading && product}}">
  <text class="section-title">商品详情</text>
  <view class="description-content">
    <rich-text nodes="{{product.detailInfo}}"></rich-text>
  </view>
</view>

<!-- 商品标签 -->
<view class="product-tags" wx:if="{{product.tags}}">
  <text class="section-title">商品标签</text>
  <view class="tags-content">
    <text class="tag" wx:for="{{product.tags.split(',')}}" wx:key="*this">{{item}}</text>
  </view>
</view>

<!-- 品牌信息 -->
<view class="brand-info" wx:if="{{product.brand}}">
  <text class="section-title">品牌</text>
  <view class="brand-content">
    <text>{{product.brand}}</text>
  </view>
</view>

<!-- 底部操作栏 -->
<view class="action-bar" wx:if="{{!loading && product}}">
  <view class="action-left">
    <button class="action-btn" bindtap="addToFavorites">
      <text>❤️</text>
      <text>收藏</text>
    </button>
    <button class="action-btn" bindtap="goToCart">
      <text>🛒</text>
      <text>购物车</text>
    </button>
  </view>
  <view class="action-right">
    <button class="add-cart-btn" bindtap="addToCart">加入购物车</button>
    <button class="buy-now-btn" bindtap="buyNow">立即购买</button>
  </view>
</view>